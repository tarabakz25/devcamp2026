import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "ディスカッション — Roomi",
};

export default function DiscussionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
